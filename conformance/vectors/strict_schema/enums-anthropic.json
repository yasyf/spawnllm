{
  "name": "enums-anthropic",
  "op": "strict_schema",
  "input": {
    "dialect": "anthropic",
    "schema": {
      "$defs": {
        "Color": {
          "enum": [
            "red",
            "blue"
          ],
          "title": "Color",
          "type": "string"
        }
      },
      "properties": {
        "color": {
          "$ref": "#/$defs/Color"
        }
      },
      "required": [
        "color"
      ],
      "title": "Enums",
      "type": "object"
    }
  },
  "expected": {
    "schema": {
      "$defs": {
        "Color": {
          "type": "string",
          "enum": [
            "red",
            "blue"
          ],
          "title": "Color"
        }
      },
      "type": "object",
      "title": "Enums",
      "properties": {
        "color": {
          "$ref": "#/$defs/Color"
        }
      },
      "additionalProperties": false,
      "required": [
        "color"
      ]
    }
  }
}
