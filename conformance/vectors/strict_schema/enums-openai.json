{
  "name": "enums-openai",
  "op": "strict_schema",
  "input": {
    "dialect": "openai",
    "schema": {
      "$defs": {
        "Color": {
          "enum": [
            "red",
            "blue"
          ],
          "title": "Color",
          "type": "string"
        }
      },
      "properties": {
        "color": {
          "$ref": "#/$defs/Color"
        }
      },
      "required": [
        "color"
      ],
      "title": "Enums",
      "type": "object"
    }
  },
  "expected": {
    "schema": {
      "$defs": {
        "Color": {
          "enum": [
            "red",
            "blue"
          ],
          "title": "Color",
          "type": "string"
        }
      },
      "properties": {
        "color": {
          "$ref": "#/$defs/Color"
        }
      },
      "required": [
        "color"
      ],
      "title": "Enums",
      "type": "object",
      "additionalProperties": false
    }
  }
}
