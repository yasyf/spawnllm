[package]
name = "spawnllm-wasm"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "cdylib wrapper exposing spawnllm-core's dispatch over a wasm32-wasip1 boundary."
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
spawnllm-core = { path = "../spawnllm-core" }
